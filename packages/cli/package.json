{
  "name": "@memory-engine/cli",
  "version": "0.1.0",
  "description": "CLI for Memory Engine",
  "private": true,
  "type": "module",
  "bin": {
    "me": "./index.ts"
  },
  "scripts": {
    "dev": "bun run index.ts",
    "build": "bun build --compile index.ts --outfile dist/me"
  },
  "dependencies": {
    "@clack/prompts": "^0.9.0",
    "@memory-engine/client": "workspace:*",
    "@memory-engine/protocol": "workspace:*",
    "commander": "^14.0.0",
    "yaml": "^2.7.0"
  }
}
